# Blank context lines in unified diffs require a leading space. Do not strip
# that syntax or change the checksummed evaluator patch during formatting.
integrations/alphabench/*.patch whitespace=-blank-at-eol
